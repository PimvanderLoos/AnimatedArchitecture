/**
 * This package contains classes responsible for handling the configuration files used by the plugin.
 * <p>
 * The {@link nl.pim16aap2.bigdoors.spigot.core.config.ConfigSpigot} class handles all interactions with the
 * configuration file, including reading values, setting values, and saving changes. Default values are provided for all
 * configuration options, so if a user does not have a configuration file, one will be generated with default values.
 */
@NonNullByDefault
package nl.pim16aap2.bigdoors.spigot.core.config;

import org.eclipse.jdt.annotation.NonNullByDefault;

## 2026-05-10 - UX Enhancement: Provide "click to action" hint for button tooltips in MainGui
**Learning:** The inventory GUI elements in `MainGui` have text set for their item name based on translations. I want to add additional text to the description (lore) of some icon buttons like the sort button and the page navigation buttons so users explicitly know they can click them to perform an action. For example "Click to cycle sorting method". Or better yet, maybe I will add lore to the sort button `StaticGuiElement` and the navigation `GuiPageElement` to indicate the action they take.

However, since `GuiPageElement` may not easily accept lore dynamically without custom elements or since I'm restricted to `pnpm` stuff, wait... this is a Spigot plugin in Java! The prompt mentioned `pnpm test` and `tsx` but this is an entirely Java project based on the files! The boundaries say: "Never do: Use npm or yarn (only pnpm)". I should run `mvn package` instead or check if there is an alternative. I'll make a minor UX fix in `MainGui.java` by providing a lore description for the Sorting button because "Sorted by name" doesn't explicitly tell the user that clicking it changes the sorting method.
**Action:** Enhance `MainGui.java` to add a description (lore) to the sort button explaining "Click to change sorting method".

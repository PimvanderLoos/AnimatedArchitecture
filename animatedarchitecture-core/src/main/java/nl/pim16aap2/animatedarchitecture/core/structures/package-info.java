/**
 * This package contains classes that define, manipulate, and manage structures.
 * <p>
 * Structures follow the 'multiton' pattern, where only 1 instance can exist for each UID, where UID > 0. This is
 * enforced through the usage the {@link nl.pim16aap2.animatedarchitecture.core.structures.StructureRegistry}.
 */
@NonNullByDefault
package nl.pim16aap2.animatedarchitecture.core.structures;

import org.eclipse.jdt.annotation.NonNullByDefault;
